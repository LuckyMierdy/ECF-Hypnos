{% extends 'base.html.twig' %}

{% block title %}Edit Hotel
{% endblock %}

{% block body %}
	<div class="d-flex flex-column align-items-center p-5" style="width:100%;">
		<h1>Edit Hotel</h1>

		{{ include('hotel/_form.html.twig', {'button_label': 'Update'}) }}
		{{ include('hotel/_delete_form.html.twig') }}
		<a href="{{ path('app_hotel_index') }}" class="link primary-link mt-3">Retour à la liste</a>
	</div>
{% endblock %}
